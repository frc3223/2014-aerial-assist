#ifndef CONTROLLER
#define CONTROLLER

#define JOYSTICK_H Joystick *leftjoy; Joystick *rightjoy;

#define LEFTJOYX  (oi->leftjoy->GetRawAxis(1))
#define LEFTJOYY  (oi->leftjoy->GetRawAxis(2))
#define RIGHTJOYX (oi->rightjoy->GetRawAxis(1))
#define RIGHTJOYY (oi->rightjoy->GetRawAxis(2))

#endif
